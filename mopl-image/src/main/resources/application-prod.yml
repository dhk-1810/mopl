server:
  port: 8087

spring:
  config:
    activate:
      on-profile: prod
  datasource:
    image:
      url: jdbc:postgresql://${DB_ENDPOINT}:5432/${DB_NAME_IMAGE}
      username: ${DB_USERNAME}
      password: ${DB_PASSWORD}
      driver-class-name: org.postgresql.Driver

  jpa:
    image:
      hibernate:
        ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      show-sql: false
