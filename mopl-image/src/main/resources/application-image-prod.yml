spring:
  datasource:
    image:
      url: jdbc:postgresql://${DB_ENDPOINT_IMAGE}:5432/${DB_NAME_IMAGE}
      username: ${DB_USERNAME_IMAGE}
      password: ${DB_PASSWORD_IMAGE}
      driver-class-name: org.postgresql.Driver

  jpa:
    image:
      hibernate:
        ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      show-sql: true
