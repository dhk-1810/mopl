services:
  mopl-gateway-app:
    container_name: mopl-gateway-app
    build:
      context: ..
      dockerfile: mopl-gateway/Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M
