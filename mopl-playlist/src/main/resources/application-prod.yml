server:
  port: 8084

spring:
  config:
    activate:
      on-profile: prod
  datasource:
    playlist:
      url: jdbc:postgresql://${DB_ENDPOINT}:5432/${DB_NAME_PLAYLIST}
      username: ${DB_USERNAME}
      password: ${DB_PASSWORD}
      driver-class-name: org.postgresql.Driver
  jpa:
    playlist:
      hibernate:
        ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      show-sql: false